<%- include('../includes/head.ejs') %>
<%- include('../includes/header.ejs') %>
<%- include('../includes/navbar.ejs') %>

<style>
    .success-container {
        max-width: 1200px;
        margin: 50px auto;
        min-height: 70vh;
        display: flex;
        align-items: center;
        padding: 0 20px;
    }
    .success-wrapper {
        width: 100%;
        display: flex;
        justify-content: center;
        text-align: center;
    }
    .success-card {
        width: 600px;
        background: white;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
        padding: 40px;
    }
    .success-icon {
        color: #4CAF50;
        font-size: 64px;
        margin-bottom: 20px;
    }
    .success-title {
        color: #2c3e50;
        font-size: 28px;
        margin-bottom: 15px;
    }
    .success-message {
        color: #576574;
        margin-bottom: 30px;
        line-height: 1.6;
    }
    .back-button {
        background-color: #d3a033;
        color: white;
        text-decoration: none;
        padding: 12px 30px;
        border-radius: 5px;
        font-weight: bold;
        display: inline-block;
        transition: all 0.3s ease;
    }
    .back-button:hover {
        background-color: #b88928;
        transform: translateY(-2px);
    }
</style>

<div class="success-container">
    <div class="success-wrapper">
        <div class="success-card">
            <div class="success-icon">
                <i class="fa fa-check-circle"></i>
            </div>
            <h2 class="success-title">Subscription Activated!</h2>
            <p class="success-message">
                Thank you for subscribing to our <%= plan.name %> plan. Your subscription is now active
                and will be valid until <%= new Date(user.subscription.endDate).toLocaleDateString() %>.
            </p>
            <a href="/plans" class="back-button">Back to Plans</a>
        </div>
    </div>
</div>

<%- include('../includes/footer.ejs') %>